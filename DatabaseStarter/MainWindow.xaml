<Window x:Class="DatabaseStarter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:DatabaseStarter.Converters"
        xmlns:vm="clr-namespace:DatabaseStarter.ViewModels"
        xmlns:res="clr-namespace:DatabaseStarter.Resources"
        mc:Ignorable="d"
        Title="{x:Static res:Strings.WindowTitle}"
        Icon="DatabaseStarter.ico"
        Height="800" Width="900"
        MinHeight="600" MinWidth="700"
        WindowStartupLocation="CenterScreen"
        Background="#FF1E1E2E">

    <Window.Resources>
        <converters:StatusToBrushConverter x:Key="StatusToBrushConverter" />
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:InverseBoolConverter x:Key="InverseBoolConverter" />
        <converters:EngineToAccentBrushConverter x:Key="EngineToAccentBrushConverter" />

        <!-- Modern Pill Button Style -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="MinWidth" Value="100" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Padding" Value="14,5" />
            <Setter Property="FontSize" Value="11.5" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1" />
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.35" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.04" ScaleY="1.04" />
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="btnBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.4"
                                                          Color="{Binding Background.(SolidColorBrush.Color), RelativeSource={RelativeSource TemplatedParent}}" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="btnBorder" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.97" ScaleY="0.97" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InstallButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#FF43A047" />
        </Style>
        <Style x:Key="StartButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#FF1E88E5" />
        </Style>
        <Style x:Key="StopButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#FFFB8C00" />
        </Style>
        <Style x:Key="UninstallButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#FFE53935" />
        </Style>

        <!-- Modern ProgressBar Style -->
        <Style x:Key="ModernProgressBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="6" />
            <Setter Property="Minimum" Value="0" />
            <Setter Property="Maximum" Value="100" />
            <Setter Property="Foreground" Value="#FF89B4FA" />
            <Setter Property="Background" Value="#FF3A3A4E" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border CornerRadius="3" Background="{TemplateBinding Background}"
                                SnapsToDevicePixels="True">
                            <Border x:Name="PART_Track">
                                <Border x:Name="PART_Indicator"
                                        CornerRadius="3"
                                        HorizontalAlignment="Left"
                                        Background="{TemplateBinding Foreground}"
                                        SnapsToDevicePixels="True">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="8" ShadowDepth="0"
                                                          Opacity="0.5" Color="#FF89B4FA" />
                                    </Border.Effect>
                                </Border>
                            </Border>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#FFCDD6F4" />
            <Setter Property="Background" Value="#FF45475A" />
            <Setter Property="BorderBrush" Value="#FF585B70" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="6,0,0,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <BulletDecorator Background="Transparent" SnapsToDevicePixels="true">
                            <BulletDecorator.Bullet>
                                <Border x:Name="checkBoxBorder"
                                        CornerRadius="3"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Width="16" Height="16">
                                    <Path x:Name="optionMark"
                                          Fill="#FFCDD6F4"
                                          Margin="2"
                                          Data="M 0,4 L 3,7 L 9,0 L 9,2 L 3,9 L 0,6 Z"
                                          Stretch="Uniform"
                                          Opacity="0" />
                                </Border>
                            </BulletDecorator.Bullet>
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              RecognizesAccessKey="True"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </BulletDecorator>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="checkBoxBorder" Property="Background" Value="#FF585B70" />
                                <Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="#FF89B4FA" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="true">
                                <Setter TargetName="optionMark" Property="Opacity" Value="1" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="{x:Null}">
                                <Setter TargetName="optionMark" Property="Data" Value="M 0,7 L 7,7 L 7,0 L 0,0 Z" />
                                <Setter TargetName="optionMark" Property="Opacity" Value="1" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark ComboBoxItem Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#FFCDD6F4" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF585B70" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF89B4FA" />
                                <Setter Property="Foreground" Value="#FF11111B" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#FF45475A" />
            <Setter Property="Foreground" Value="#FFCDD6F4" />
            <Setter Property="BorderBrush" Value="#FF585B70" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="6,3" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid x:Name="MainGrid" SnapsToDevicePixels="True">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition MinWidth="{x:Static SystemParameters.VerticalScrollBarWidth}"
                                                  Width="0" />
                            </Grid.ColumnDefinitions>
                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Grid.ColumnSpan="2"
                                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   Margin="1"
                                   PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}"
                                   Placement="Bottom">
                                <Border x:Name="DropDownBorder"
                                        Background="#FF313244"
                                        BorderBrush="#FF585B70"
                                        BorderThickness="1"
                                        CornerRadius="5"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        MinWidth="{Binding ActualWidth, ElementName=MainGrid}">
                                    <ScrollViewer x:Name="DropDownScrollViewer">
                                        <Grid x:Name="Grid" RenderOptions.ClearTypeHint="Enabled">
                                            <Canvas x:Name="Canvas" HorizontalAlignment="Left" Height="0"
                                                    VerticalAlignment="Top" Width="0">
                                                <Rectangle x:Name="OpaqueRect"
                                                           Fill="{Binding Background, ElementName=DropDownBorder}"
                                                           Height="{Binding ActualHeight, ElementName=DropDownBorder}"
                                                           Width="{Binding ActualWidth, ElementName=DropDownBorder}" />
                                            </Canvas>
                                            <ItemsPresenter x:Name="ItemsPresenter"
                                                            KeyboardNavigation.DirectionalNavigation="Contained"
                                                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                        </Grid>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                            <ToggleButton x:Name="ToggleButton"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          Grid.ColumnSpan="2"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="templateRoot"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="5"
                                                SnapsToDevicePixels="True">
                                            <Border x:Name="splitBorder"
                                                    BorderBrush="Transparent"
                                                    BorderThickness="1"
                                                    HorizontalAlignment="Right"
                                                    Margin="0"
                                                    SnapsToDevicePixels="True"
                                                    Width="{x:Static SystemParameters.VerticalScrollBarWidth}">
                                                <Path x:Name="arrow"
                                                      Data="M 0,0 L 3.5,4 L 7,0 Z"
                                                      Fill="#FFCDD6F4"
                                                      HorizontalAlignment="Center"
                                                      Margin="0"
                                                      VerticalAlignment="Center" />
                                            </Border>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="templateRoot" Property="Background"
                                                        Value="#FF585B70" />
                                                <Setter TargetName="templateRoot" Property="BorderBrush"
                                                        Value="#FF89B4FA" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="contentPresenter"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              IsHitTestVisible="False"
                                              Margin="{TemplateBinding Padding}"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="DropDownBorder" Property="Height" Value="95" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FF181825" Padding="24,12">
            <DockPanel>
                <Image DockPanel.Dock="Left" Source="DatabaseStarter.jpg"
                       Width="48" Height="48" Margin="0,0,16,0"
                       VerticalAlignment="Center"
                       RenderOptions.BitmapScalingMode="HighQuality" />

                <!-- Info Button (right-aligned) -->
                <Button DockPanel.Dock="Right" Click="InfoButton_Click"
                        VerticalAlignment="Center" Cursor="Hand"
                        Width="30" Height="30" Margin="8,0,0,0"
                        ToolTip="{x:Static res:Strings.InfoButtonTooltip}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="infoBorder"
                                    Background="#FF313244" CornerRadius="15"
                                    Width="30" Height="30">
                                <TextBlock Text="i" FontSize="16" FontWeight="Bold"
                                           FontStyle="Italic" Foreground="#FF89B4FA"
                                           HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="infoBorder" Property="Background" Value="#FF45475A" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="infoBorder" Property="Background" Value="#FF585B70" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Language Toggle Button (right-aligned) -->
                <Button DockPanel.Dock="Right"
                        Command="{Binding ToggleLanguageCommand}"
                        Content="{Binding LanguageToggleLabel}"
                        VerticalAlignment="Center" Cursor="Hand"
                        Margin="8,0,0,0"
                        FontSize="12" FontWeight="SemiBold"
                        Foreground="#FFCDD6F4"
                        ToolTip="Deutsch / English">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="langBorder"
                                    Background="#FF313244" CornerRadius="8"
                                    Padding="10,4">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="langBorder" Property="Background" Value="#FF45475A" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="langBorder" Property="Background" Value="#FF585B70" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Database Starter"
                               FontSize="24" FontWeight="Bold"
                               Foreground="#FFCDD6F4" />
                    <DockPanel Margin="0,4,0,0">
                        <TextBlock Text="{x:Static res:Strings.Subtitle}"
                                   FontSize="13" Foreground="#FFA6ADC8" />
                        <TextBlock Text="{Binding AppVersion, StringFormat='v.{0}'}"
                                   FontSize="13" Foreground="#FF6C7086"
                                   HorizontalAlignment="Right" Margin="12,0,0,0" />
                    </DockPanel>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Database Cards -->
        <ItemsControl Grid.Row="1" ItemsSource="{Binding Databases}" Margin="16,8">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="3" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:DatabaseViewModel}">
                    <!-- Outer card with shadow -->
                    <Border CornerRadius="14" Margin="6,5" VerticalAlignment="Stretch"
                            BorderBrush="#FF3A3A4E" BorderThickness="1">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="20" ShadowDepth="3"
                                              Opacity="0.35" Color="Black" Direction="270" />
                        </Border.Effect>
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0.7">
                                <GradientStop Color="#FF262637" Offset="0" />
                                <GradientStop Color="#FF313244" Offset="0.4" />
                                <GradientStop Color="#FF363649" Offset="1" />
                            </LinearGradientBrush>
                        </Border.Background>

                        <!-- Inner clip border -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="5" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!-- Left accent gradient strip -->
                            <Border Grid.Column="0" CornerRadius="14,0,0,14"
                                    Background="{Binding InstanceInfo.Engine,
                                        Converter={StaticResource EngineToAccentBrushConverter}}" />

                            <!-- Main content -->
                            <Grid Grid.Column="1" Margin="20,14,20,10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <!-- Row 0: Header — Icon + Name + Status Badge -->
                                <DockPanel Grid.Row="0">
                                    <!-- Status pill badge (right) -->
                                    <Border DockPanel.Dock="Right" CornerRadius="10"
                                            Padding="10,4" VerticalAlignment="Center"
                                            Background="#25FFFFFF">
                                        <StackPanel Orientation="Horizontal">
                                            <Ellipse Width="8" Height="8" Margin="0,0,6,0"
                                                     VerticalAlignment="Center"
                                                     Fill="{Binding Status, Converter={StaticResource StatusToBrushConverter}}" />
                                            <TextBlock Text="{Binding StatusText}"
                                                       FontSize="11" FontWeight="SemiBold"
                                                       Foreground="#FFBAC2DE"
                                                       VerticalAlignment="Center" />
                                        </StackPanel>
                                    </Border>

                                    <!-- Engine icon + name -->
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding EngineIcon, Mode=OneWay}"
                                                   FontSize="28" VerticalAlignment="Center"
                                                   Foreground="#FFCDD6F4" />
                                        <TextBlock Text="{Binding EngineName, Mode=OneWay}"
                                                   FontSize="20" FontWeight="Bold"
                                                   Foreground="#FFCDD6F4"
                                                   Margin="12,0,0,0" VerticalAlignment="Center" />
                                    </StackPanel>
                                </DockPanel>

                                <!-- Row 1: Separator -->
                                <Rectangle Grid.Row="1" Height="1" Margin="0,10"
                                           Fill="#20FFFFFF" />

                                <!-- Row 2: Details — Version / Port / Path / Progress -->
                                <Grid Grid.Row="2" VerticalAlignment="Top">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Version -->
                                    <TextBlock Grid.Row="0" Grid.Column="0"
                                               Text="{x:Static res:Strings.LabelVersion}" Foreground="#FF6C7086"
                                               FontSize="12" VerticalAlignment="Center"
                                               Margin="0,0,12,6" />
                                    <StackPanel Grid.Row="0" Grid.Column="1"
                                                Orientation="Horizontal" Margin="0,0,0,6">
                                        <ComboBox ItemsSource="{Binding AvailableVersions}"
                                                  SelectedItem="{Binding SelectedVersion}"
                                                  IsEnabled="{Binding CanChangeVersion}"
                                                  DisplayMemberPath="DisplayName"
                                                  MinWidth="160" Height="24" FontSize="11"
                                                  VerticalContentAlignment="Center" />
                                        <TextBlock Text="{x:Static res:Strings.LabelVersionLocked}"
                                                   Foreground="#FF585B70"
                                                   FontSize="10" FontStyle="Italic"
                                                   VerticalAlignment="Center" Margin="8,0,0,0"
                                                   Visibility="{Binding IsInstalled, Converter={StaticResource BoolToVisibilityConverter}}" />
                                    </StackPanel>

                                    <!-- Port -->
                                    <TextBlock Grid.Row="1" Grid.Column="0"
                                               Text="{x:Static res:Strings.LabelPort}" Foreground="#FF6C7086"
                                               FontSize="12" VerticalAlignment="Center"
                                               Margin="0,0,12,4" />
                                    <TextBlock Grid.Row="1" Grid.Column="1"
                                               Text="{Binding Port, Mode=OneWay}"
                                               Foreground="#FFCDD6F4" FontSize="12"
                                               FontWeight="SemiBold" VerticalAlignment="Center"
                                               Margin="0,0,0,4" />

                                    <!-- Path + Progress -->
                                    <TextBlock Grid.Row="2" Grid.Column="0"
                                               Text="{x:Static res:Strings.LabelPath}" Foreground="#FF6C7086"
                                               FontSize="12" VerticalAlignment="Center"
                                               Margin="0,0,12,0" />
                                    <DockPanel Grid.Row="2" Grid.Column="1">
                                        <!-- Progress (right) -->
                                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal"
                                                    VerticalAlignment="Center"
                                                    Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"
                                                    Margin="14,0,0,0">
                                            <ProgressBar Value="{Binding DownloadProgress}"
                                                         Width="110"
                                                         Style="{StaticResource ModernProgressBar}"
                                                         VerticalAlignment="Center" />
                                            <TextBlock Text="{Binding StatusMessage}"
                                                       Foreground="#FFA6ADC8" FontSize="10"
                                                       Margin="8,0,0,0" VerticalAlignment="Center"
                                                       MaxWidth="160" TextTrimming="CharacterEllipsis" />
                                        </StackPanel>
                                        <!-- Path text -->
                                        <TextBlock Text="{Binding InstallPath, Mode=OneWay}"
                                                   Foreground="#FF89B4FA" FontSize="11"
                                                   VerticalAlignment="Center"
                                                   TextTrimming="CharacterEllipsis" />
                                    </DockPanel>
                                </Grid>

                                <!-- Row 3: Separator -->
                                <Rectangle Grid.Row="3" Height="1" Margin="0,8"
                                           Fill="#20FFFFFF" />

                                <!-- Row 4: Action buttons + Log expander -->
                                <DockPanel Grid.Row="4">
                                    <!-- Log expander (right) -->
                                    <Expander DockPanel.Dock="Right" Header="{x:Static res:Strings.ExpanderLog}"
                                              Foreground="#FF6C7086" FontSize="11"
                                              IsExpanded="False" VerticalAlignment="Center"
                                              Margin="8,0,0,0">
                                        <TextBox Text="{Binding LogOutput, Mode=OneWay}"
                                                 IsReadOnly="True" TextWrapping="Wrap"
                                                 MaxHeight="80" MaxWidth="300"
                                                 VerticalScrollBarVisibility="Auto"
                                                 Background="#FF181825" Foreground="#FFA6ADC8"
                                                 BorderThickness="0" FontFamily="Consolas"
                                                 FontSize="10" Padding="6" Margin="0,4,0,0" />
                                    </Expander>

                                    <!-- Action buttons -->
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Content="{x:Static res:Strings.ButtonInstall}"
                                                Style="{StaticResource InstallButton}"
                                                Command="{Binding InstallCommand}" />
                                        <Button Content="{x:Static res:Strings.ButtonStart}"
                                                Style="{StaticResource StartButton}"
                                                Command="{Binding StartCommand}" />
                                        <Button Content="{x:Static res:Strings.ButtonStop}"
                                                Style="{StaticResource StopButton}"
                                                Command="{Binding StopCommand}" />
                                        <Button Content="{x:Static res:Strings.ButtonUninstall}"
                                                Style="{StaticResource UninstallButton}"
                                                Command="{Binding UninstallCommand}" />
                                    </StackPanel>
                                </DockPanel>
                            </Grid>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>